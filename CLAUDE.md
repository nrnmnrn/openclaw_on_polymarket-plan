# TraderClaw — Claude 工作規範

## 專案性質

此專案**僅用於規劃**，不會在此建立任何程式碼。
最終規劃成果將移交至獨立的實作專案。

## 討論與決策原則

無論是 Claude 的提案或用戶的想法，每次討論與文件修改都必須主動審視：

1. **成本** — 方案的 API 費用、基礎建設成本、維護負擔是否合理？
2. **規模可控** — 此為個人開發專案，所有設計必須讓一個人能獨立掌控與維運。
3. **可行性優先** — 先確認能不能做到，再考慮做得多好；避免天馬行空的方向。
4. **即時設計審查** — 對話中出現新想法或設計決策時，Claude 立即對照設計原則進行審查，依「問題嚴重度 × 修改代價」矩陣輸出評估。問題小且修改代價大的觀察略過（避免過度工程化）。

## 文件撰寫規範

- 流程圖一律使用 Mermaid 語法（```mermaid 程式碼區塊）
- 表格一律使用 Markdown 語法
- **禁止使用 ASCII art**（浪費 token，影響可讀性）

## 每次對話起點

1. 先讀 `CURRENT_SPEC.md`（系統唯一真實來源）
2. 需要特定決策細節時，再讀對應的 `adr/ADR-XXX-*.md`
3. `archive/` 為原始討論文件封存區，已過時，不需重讀

## CURRENT_SPEC.md 維護規則

決策落定後**覆寫**更新，不追加歷史。

## ADR 維護規則

- ADR 主體永遠反映**當前最佳決策**（覆寫更新）
- 底部保留輕量 `## Revision Log` 表格，欄位：**日期 + 做了什麼 + 為什麼**（三欄）
- 完整歷史由 git 保存，ADR 本身不存舊內容
- ADR 數量不固定，可依需求新增

## 目錄結構

| 路徑 | 用途 |
|------|------|
| `CURRENT_SPEC.md` | 主要規格書，每次對話從此讀起 |
| `adr/` | 架構決策記錄（ADR-001 起，可新增）|
| `archive/` | 原始討論文件（封存，不修改）|

## Idea Review 行為說明

對話中出現以下任一特徵時，Claude 自動執行 idea-review 評估：
- 提出新想法、新元件、新功能、新工作流
- 「我想要 X」、「可以考慮 X」、「如果 X」等陳述
- 涉及成本、頻率、複雜度的設計選擇

### 檢核維度

| 維度 | 問題 |
|------|------|
| 成本 | API / 基礎建設 / 維護費用是否合理？ |
| 規模可控 | 一個人能獨立掌控與維運嗎？ |
| 可行性優先 | 先確認能做到，再問做得多好 |
| 可程式化機會 | 有沒有目前用 LLM 做的事，可以用 SQL / 規則 / 演算法 / 確定性程式取代？ |

### 輸出規則

- 依「問題嚴重度 × 修改代價」輸出，四個象限：
  - 問題大 + 修改小 → `[必改]`
  - 問題大 + 修改大 → `[需決策]`
  - 問題小 + 修改小 → `[建議]`
  - 問題小 + 修改大 → 靜默（避免過度工程化）
- 同一維度可輸出多個問題（不限一條）
- 有發現問題才輸出對應行；全部略過時整個卡片不出現（靜默）
- 卡片插在回應末尾，不取代主要回答

### 輸出格式

```
[Idea Review]
[必改]   成本: 每次觸發 Sonnet 費用過高 → 改用 Qwen 即可解決
[必改]   成本: 需額外開 Redis 實例，每月 $15 固定成本 → 評估是否必要
[需決策] 規模可控: 需維護 2 個獨立服務，個人負擔邊界 → 是否接受？
[建議]   可行性: 依賴 Tavily quota，需確認免費層上限是否足夠
```
